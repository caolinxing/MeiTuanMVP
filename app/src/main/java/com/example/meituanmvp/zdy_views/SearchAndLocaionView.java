package com.example.meituanmvp.zdy_views;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class SearchAndLocaionView extends LinearLayout {
    public SearchAndLocaionView(Context context) {
        super(context);
        //初始化
        init(context);
    }

    public SearchAndLocaionView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
        //初始化
        init(context);
    }

    public SearchAndLocaionView(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        //初始化
        init(context);
    }

    private void init(Context context) {

    }

}
